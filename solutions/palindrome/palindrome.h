#pragma once

#include <string>

/** \brief Checks whether the provided text is a palindrome.
 *
 * The check is case-sensitive and does not skip over whitespace or special
 * characters.
 */
bool is_palindrome(std::string text);